// In JavaScript, regular expressions are used to match patterns in strings, regex are great for validation, searching and replacing text.

//There are 2 ways to create regex :

//1. Literal syntax
let regex = /pattern/flags;

//2. Constructor
let regex2 = new RegExp("pattern", "flags");

