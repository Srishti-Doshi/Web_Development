/*

A module is simply a file that exports or imports something.


export → makes the function available outside the file




Named export: 

export function add()
import { add }

You export multiple things with their names.
Import: You must use the same names.







Default export:

export default function add()
import add

Used when the file has one main thing to export.
Name can be anything when importing
*/