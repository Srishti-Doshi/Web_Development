TYPESCRIPT DOMINATION

TypeScript => improved version of JavaScript and has more features than JS
TypeScript : provides Type Safety


The below code is valid in JS but invalid in TS:
var x = 22;
var y = 23;
var x = 23;

JS was created in 15 days and has many issues , missing features and has no type safety
var a = 12;
a = "sri";

Setting up environment for TS
Need:   1.Browser(Brave, Chrome)
        2.Nodejs(helps : TS code compiles into JS code)
        3.TypeScript => install ts compiler(globally for eveywhere at laptop)
                    in terminal run "npm i typescript -g"
                    if don't work use "sudo npm i typescript -g"

                    use "tsc filename.ts" to run the TS file and it will compile to a JS file

        4.VS code and its Extensions
                    ESLint
                    JavaScript and TypeScript Nightly
                    Live Server
                    Material Icon Theme
                    Prettier - Code formatter
                    Prettier ESLint
                    Pretty TypeScript Errors


tips: ctrl + backtick opens Terminal

typescript Setup

    Setting up a TypeScript project 
        => create a ts file

    Configuring tsconfig.json (rules for compiling)
        => in tsconfig file we do settings like (var a; not used variables, function that returns null etc) are to treat as erors
        => run "tsc --init" or "npx tsc --init" to get tsconfig file

    Compiling TypeScript
        => run "tsc" or "tsc filename.ts"
        => run "tsc --watch" for continuation compilation of code as writing and saving

npm --v
node --v
tsc --v

name of file is in italic => save it

auto save option

DataType
    number => 12, 12.3, 12.23553
    character => c, a, d
    string => apple, Srishti
    boolean => true, false

In TypeScript
    primitives and reference
        a = 12
        b = a
        b + 2
        b => 14
        a => 12

        a = [1,2,3,4,5]
        b = a
        b => remove last number => [1,2,3,4]
        a => [1,2,3,4]

        [] {} () => type => reference => any change leads to change in parent

        no brackets => primitive

        12      => primtive
        Prince  => primtive
        true    => primtive
        []      => reference
        {}      => reference
        "apple" => primtive
        false   => primtive
        12.34   => primtive
        ()      => reference
        var a = true => primitive
        var [2,4]    => reference

        primitive => can be copied directly
        reference => cannot be copied directly, other ways

        var a = [1,2,3,4]
        var b = a => here b does not copy a , it has a reference of a 

Basic types
    Primitive types(number, string, boolean)
    Arrays
    Tuples
    Enums
    Any, Unknown, Void, Null, Undefined, Never


to run use "node app.js"






export {}
   ↓
makes the file a module
   ↓
prevents global variable conflicts

This file is a module.
Do NOT treat it as a global script.



var → one shared variable
let → new variable per block/iteration


var => function scope, can be redeclare, can be reassign

let => block scope, cannot be redeclare, can be reassign

const => block scope, cannot be redeclare, cannot be reassign, must be initialized



Number	number	Double precision 64-bit floating point values
String	string	Sequence of Unicode characters
Boolean	boolean	Logical values, true and false
Void	void	For functions that do not return a value
Null	null	Intentional absence of an object value
Undefined	undefined	Value given to uninitialized variables
Symbol	symbol	Unique and immutable primitive for unique object keys
Object	object	Instances of classes, arrays, functions, etc.
Never	never	Values that never occur (e.g., a function that always throws)

for (let key in object){}
for ..in => give indexes (specially useful for objects) => value : object[key]


for (let value of array){}
for ... of => give values (specially useful for arrays)



Parameter Order 
1. Required
2. Optional
3. Default

Required → Optional (strict)
Default parameters : Can be anywhere (but better to keep at end)





IoC 
1. Install package
Run this in terminal: npm install typedi reflect-metadata

2. Import Service
At top of your file: import { Service } from "typedi";

3. Add this in your main file (VERY IMPORTANT)
import "reflect-metadata";

4. Enable in tsconfig.json
{
  "experimentalDecorators": true,
  "emitDecoratorMetadata": true
}