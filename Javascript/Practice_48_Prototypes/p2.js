//In js array is an object internally

let arr = ["apple", "mango", "banana"]; //also gets an array type prototype

//no need to create any functions we can use protype functions

arr.push("litchi"); //push etc methods comes from prototype object

console.log(arr);
